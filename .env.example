# Safe configuration example only. Do not place passwords, tokens, AWS access
# keys, secret keys, diary content, or other credentials in environment files.
AWS_BACKUP_API_ENDPOINT=https://replace-me.execute-api.region.amazonaws.com/v2/backup/presign
AWS_COGNITO_REGION=us-east-1
AWS_COGNITO_CLIENT_ID=replace-with-public-cognito-app-client-id
